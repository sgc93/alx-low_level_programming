#include<stdio.h>
/**
 * main - is the entry for the execution
 * Return must be always 0
 */
int main(void)
{
        puts("\"Programming is like building a multilingual puzzle");
        return (0);
}
